package com.beehive.dashboard.types.bank;

/**
 * Enum representing types of bank account movements.
 * Used to distinguish between expenses and income transactions within the application.
 * Typical usage includes categorizing movements for reporting, analytics, and business logic.
 */
public enum MovementType {
    EXPENSE,
    INCOME
}
