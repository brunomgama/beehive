package com.beehive.dashboard.types.bank;

/**
 * Enum representing types of bank accounts within the application.
 * Used to categorize accounts for banking operations, reporting, and business logic.
 * Typical usage includes account creation, filtering, and validation.
 */
public enum AccountType {
    CURRENT,
    SAVINGS,
    INVESTMENTS,
    CLOSED
}