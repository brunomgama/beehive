package com.beehive.dashboard.types.authentication;

/**
 * Enum representing user roles within the application.
 * Used to define access levels and permissions for authentication and authorization.
 * Typical usage includes assigning roles to users and restricting access to certain features.
 */
public enum Role {
    USER,
    ADMINISTRATOR
}